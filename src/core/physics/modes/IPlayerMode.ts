/**
 * Strategy pattern interface for player modes.
 * Each mode (Cube, Ship, Ball) implements this interface.
 */

export type {
  IPlayerMode,
  PhysicsState,
  InputState,
  PhysicsConfig,
} from '../types';
